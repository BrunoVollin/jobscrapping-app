{
  "name": "js-app",
  "version": "1.0.0",
  "scripts": {
    "start": "expo start --dev-client",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "test": "jest",
    "build": "PACKAGE_VERSION=$(node -p -e \"require('./package.json').version\") && cd android && ENVFILE=.env.prd ./gradlew assembleRelease && mv app/build/outputs/apk/release/app-release.apk ../app-releaseProd-v.$PACKAGE_VERSION.apk"
  },
  "jest": {
    "preset": "react-native"
  },
  "dependencies": {
    "expo": "~47.0.3",
    "expo-splash-screen": "~0.17.4",
    "expo-status-bar": "~1.4.2",
    "jest": "^29.3.1",
    "react": "18.1.0",
    "react-native": "0.70.5"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9"
  },
  "private": true
}
